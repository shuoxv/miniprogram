<view class="tabs">
    <view class="tabs_title">
        <view 
        wx:for="{{tabs}}"
        wx:key="id"
        bindtap="handleitemtype"
        data-index="{{index}}"
        class="title_item  {{item.isActive?'active':''}}">{{item.name}}
        </view>
    </view>
    <view class="tabs_content">内容</view>
</view>